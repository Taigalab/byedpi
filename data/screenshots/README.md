# Screenshots

- `main.png` — the main window (bypass toggle, DNS selector, TTL slider,
  technique switches). Referenced from `README.md` and the AppStream metainfo
  (`data/io.github.byedpi.ByeDPI.metainfo.xml`).

Regenerate by launching the app and cropping to the window, then replacing
`main.png`. Keep it on a stock Adwaita theme for a consistent look.
